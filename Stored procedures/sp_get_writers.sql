CREATE PROCEDURE [dbo].[sp_get_writers]
AS
BEGIN
   /*SELECT TOP 100 PERCENT
       w.[FirstName],
	   w.[LastName],
	   w.[Email],
	   w.[PhoneNUmber],
	   w. [DateOfBirth],
	   w.[Active]
   FROM [dbo].[Writers] AS w
   INNER JOIN [dbo].[Blogs] as b
   ON w.Id = b.WriterId
   ORDER BY w.[FirstName];*/

END